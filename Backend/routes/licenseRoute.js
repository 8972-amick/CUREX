import express from "express";

const router = express.Router();

// Placeholder for license-related endpoints
router.get("/", (req, res) => {
  res.json({ message: "License route placeholder" });
});

export default router;
